# Build artifacts
finnestdb
finnestdb.exe
importkotus
importdict
importekilex
importekilexdetails
/importud
fetchekilex
reduceekilex
parsertest
parser-compare
corpusmine
autoresearch
server
/scrapegutenberg
*.db
*.db-shm
*.db-wal

# Rust
parser/target/
parser/Cargo.lock

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Frontend
web/node_modules/

# Local test artifacts
test-results/
reports/
.cache/
.venv-omorfi/

# Local corpora and manual text imports
# All third-party data artifacts go here — see docs/ARTIFACT_POLICY.md.
# Populated by scripts/setup-local.sh (Ekilex shards, Kotus sanalista,
# Gutenberg silver, etc.). Zip + share with a teammate to bootstrap fast.
localdata/

# Belt-and-braces: explicitly forbid the legacy data/ subtrees that were
# tracked before the 2026-05-07 cleanup. Anything new written to data/
# should now go to localdata/ instead.
data/ekilex/
data/kotus/
data/silver-fi/
data/silver-fi-runosto/

# UD treebank cache (cloned by scripts/fetch-and-import-ud.sh).
# ~50 MB per treebank, regenerable from the importer — don't commit.
data/ud-cache/

# Train splits of UD-derived gold are gitignored: each is 10–30 MB and
# they're used for OOV/coverage analysis, not headline eval. Regenerable
# via scripts/fetch-and-import-ud.sh.
testdata/parser-eval/fi/gold-train/
testdata/parser-eval/et/gold-train/

# Estonian UD treebanks (EDT, EWT) are CC BY-NC-SA. The derived gold JSON
# stays local — the public repo only hosts FI UD gold (CC BY / CC BY-SA).
# See cmd/importud/main.go for source attribution.
testdata/parser-eval/et/gold/ud-et-*.json

# Claude Code
.claude/

# Autoresearch transient experiment logs (commit specific runs explicitly)
experiments/test*.jsonl
experiments/autoresearch.jsonl

# Local notes and design scratch
.obsidian/
design/
How the Finnish & Estonian Parsers can learn from each other.md
