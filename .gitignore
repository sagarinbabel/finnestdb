# Build artifacts (root-only — leading slash so `importdict` ignores the
# binary at `./importdict` but NOT `cmd/importdict/` source files).
/finnestdb
/finnestdb.exe
/importkotus
/importdict
/importekilex
/importekilexdetails
/importud
/enrichgoldfeats
/fetchekilex
/reduceekilex
/parsertest
/parser-compare
/corpusmine
/autoresearch
/server
/scrapegutenberg
/fetchfrequency
/doctor
*.db
*.db-shm
*.db-wal

# Rust
parser/target/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Python caches
__pycache__/

# Frontend
web/node_modules/

# Local test artifacts
test-results/
reports/**
!reports/
!reports/**/
!reports/parser-eval/audit-*.md
.cache/
.venv-omorfi/
.venv-estnltk
.venv-estnltk/

# Single root for every gitignored runtime artifact: dictionary corpora,
# Gutenberg silver, generated lemmatizer tables, the UD treebank cache, and
# any local-only parser-eval gold (NC-licensed ET dev/test, train splits).
# See docs/ARTIFACT_POLICY.md and docs/data_enhancement.md for the full
# inventory. `tar czf finnestdb-bootstrap.tgz localdata/ finnestdb.db`
# captures the entire bootstrap state for handoff to a teammate.
localdata/

# Legacy data/ paths (pre-2026-05-07) are kept here as belt-and-braces in
# case a stale workflow still writes to them. New code MUST write to
# localdata/ instead — there is no second data root.
data/

# Claude Code
.claude/

# Autoresearch transient experiment logs (commit specific runs explicitly)
experiments/test*.jsonl
experiments/autoresearch.jsonl

# Local notes and design scratch
.obsidian/
design/
How the Finnish & Estonian Parsers can learn from each other.md
