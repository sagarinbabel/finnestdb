{
  "name": "finnestdb-web",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.9.3"
  }
}
